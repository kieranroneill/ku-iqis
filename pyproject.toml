[tool.black]
line-length = 120
target-version = ['py310']

[tool.isort]
known_first_party = ["kuiqis"]
profile = "black"

[tool.pylint."messages control"]
disable = "all"
enable = [
  "empty-docstring",
  "missing-class-docstring",
  "missing-function-docstring",
  "missing-module-docstring"
]
ignore = ["setup.py", "__init__.py"]
